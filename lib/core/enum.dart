enum NotificationType { error, warning, success, neutral }

enum OtpType { signUp, login }

enum CryptoTradeType { asset, usd }

enum ImageType { link, file }

enum Transactions {All, GiftCard,Crypto,Wallet}
enum Type { All, Buy, Sell}
enum Status { All, Success, Failed}
